/**
 * Ambient stubs for types that were globally auto-imported (Nuxt-style) in the
 * original `agents` project and are referenced — without an explicit import — by
 * several copied api modules. Declared globally here so the ported files resolve
 * unmodified. Loosely typed on purpose; tighten if/when the real shapes are needed.
 */

type GBDFLResponse = any;
type GetBankInfoResponse = any;
type SendTaskDecision = string;
type ProductNames = string;
type StatusCode = number | string;
type StatementType = string;
type AvailableSources = string;
type StandardDictionaryType = any;
type CurrencyNames = string;
type UserInfo = any;
